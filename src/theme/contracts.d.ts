import { ComponentType } from 'react';

export interface Theme {
  //colors
  colorWhite: string;
  colorBlack: string;
  colorGreen: string;
  colorViolet: string;
}

export interface ColorsInterface {
  violet?: boolean;
  green?: boolean;
  black?: boolean;
  white?: boolean;
}
